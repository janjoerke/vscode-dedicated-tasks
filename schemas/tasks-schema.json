{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "version": {
      "type": "string"
    },
    "tasks": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "label": {
            "type": "string"
          },
          "type": {
            "type": "string"
          },
          "command": {
            "type": "string"
          },
          "options": {
            "type": "object",
            "properties": {
              "dedicatedTasks": {
                "type": "object",
                "description": "Configuration for Dedicated tasks extension",
                "properties": {
                  "label": {
                    "type": "string",
                    "description": "Display label for the task in Dedicated tasks"
                  },
                  "statusbarLabel": {
                    "type": "string",
                    "description": "Shorthand label for display in status bar. Defaults to label if not set."
                  },
                  "detail": {
                    "type": "string",
                    "description": "Additional detail text shown below the task label"
                  },
                  "hide": {
                    "type": "boolean",
                    "description": "Whether to hide this task from Dedicated tasks",
                    "default": false
                  },
                  "groups": {
                    "type": "array",
                    "description": "Array of group paths. Each path is an array of strings representing a hierarchy. Tasks can appear in multiple locations.",
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "description": "Single-level group name"
                        },
                        {
                          "type": "array",
                          "description": "Multi-level group path (e.g., ['Build', 'Frontend'])",
                          "items": {
                            "type": "string"
                          },
                          "minItems": 1
                        }
                      ]
                    },
                    "minItems": 1
                  },
                  "order": {
                    "type": "number",
                    "description": "Sort order within the group (lower numbers appear first)",
                    "default": 0
                  },
                  "category": {
                    "type": "string",
                    "description": "Category for top-level task organization. Tasks with no category default to 'default'. When multiple categories exist, a dropdown appears in the tree view header.",
                    "default": "default"
                  }
                },
                "required": ["groups"]
              }
            }
          }
        }
      }
    }
  }
}
